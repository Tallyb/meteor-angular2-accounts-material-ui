import { FormControl } from '@angular/forms';
export declare function validateEmail(c: FormControl): {
    validateEmail: {
        valid: boolean;
    };
};
export declare function validatePassword(c: FormControl): {
    validatePassword: {
        valid: boolean;
    };
};
